// src/promo/scenes/history-insights.tsx
// PLACEHOLDER — Task 13 will replace this with the real scene that
// composes BrowserChrome + BiliPage + PopupShell + HistoryView.
export default function HistoryInsights() {
  return (
    <div style={{ padding: 24, fontFamily: 'monospace' }}>
      history-insights scene — placeholder (Task 13 will fill this in)
    </div>
  )
}
